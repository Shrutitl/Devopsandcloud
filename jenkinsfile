pipeline {
  agent any
  tools {
    maven 'Maven-3.6.3'
    jdk 'open-jdk17'
  }
  
stages {
stage('Part 2 - app build using maven') {
      steps {
        script {
            sh "mvn clean"
            sh "mvn compile"
            sh "mvn package"
          }
      }
    } 
   }
 }
